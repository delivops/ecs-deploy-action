# EC2 Basic Configuration Test
# Tests basic EC2 launch type with minimal configuration
name: ec2-basic-app
replica_count: 1
cpu: 512
memory: 1024
role_arn: arn:aws:iam::123456789012:role/ecsTaskExecutionRole

# EC2 launch type
launch_type: EC2
network_mode: awsvpc

port: 8080
